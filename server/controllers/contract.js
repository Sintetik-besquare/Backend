const { queryByPromise } = require('../dbconfig/db');
const validator =require('validator');

const userController = {
    
    payout: async(req,res)=>{
                //validate input
                //recalculate pricing according to contract type
                //if the input is out of the range respond with error message
        class Payout{
            constructor(time,duration,stake){

            }
        }
   
    },
}

module.exports = userController;